# Project-local Supabase config. Used by `supabase functions deploy`.
# Generated by hand for the inbound-email feature; expand as we add more functions.
project_id = "nvefrdnafsplxlawmmmb"

[functions.inbound-email]
# Postmark hits this URL with no Bearer token, only ?token=<shared secret>.
# So we disable the default verify_jwt enforcement on incoming requests.
verify_jwt = false
