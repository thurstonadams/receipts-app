// Empty stub used by jest.config.js moduleNameMapper for RN / Expo native
// modules that pure-logic tests don't actually execute. If a test does
// need a specific module surface, mock it locally with jest.mock(...).
module.exports = {};
